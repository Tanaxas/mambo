Options -MultiViews
RewriteEngine On

# Serve real files and directories normally; send application routes to the
# static entry point so direct visits such as /destinations keep working.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]
RewriteRule ^ index.html [L]
